# This is a Databricks asset bundle definition for realtime-leaderboard.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: realtime-leaderboard
  uuid: 70034e10-11cb-4de6-b603-39339f9d6678

include:
  - resources/*.yml

workspace:
  host: https://e2-demo-field-eng.cloud.databricks.com
  artifact_path: /Volumes/users/alex_vanadio/realtime_leaderboard/${bundle.name}/${bundle.target}/${workspace.current_user.short_name}

artifacts:
  default:
    type: jar
    build: sbt package && sbt assembly
    path: .
    files:
      - source: ./target/scala-2.13/realtime-leaderboard-assembly-1.0.0.jar

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/alex.vanadio@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/alex.vanadio@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: alex.vanadio@databricks.com
        level: CAN_MANAGE
